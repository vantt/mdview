{
  "name": "mdview-codemirror-bundle",
  "private": true,
  "description": "Builds crates/mdview/assets/codemirror.min.js — the CodeMirror 6 bundle the in-place Docs editor lazy-loads. Run `npm install && npm run build` here and commit the output; the daemon embeds it at compile time.",
  "scripts": {
    "build": "esbuild entry.js --bundle --minify --format=iife --target=es2018 --outfile=../../crates/mdview/assets/codemirror.min.js"
  },
  "dependencies": {
    "@codemirror/commands": "^6.8.1",
    "@codemirror/lang-markdown": "^6.3.2",
    "@codemirror/language": "^6.11.0",
    "@codemirror/search": "^6.5.10",
    "@codemirror/state": "^6.5.2",
    "@codemirror/theme-one-dark": "^6.1.2",
    "@codemirror/view": "^6.36.5",
    "codemirror": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
