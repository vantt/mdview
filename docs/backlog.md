# Product Backlog

PBI rows cho mdview. Status: `proposed` → `in-flight` → `done`.
Chất liệu cho các mục port: [docs/distillery/porting-log.md](distillery/porting-log.md).

| ID | Title | Status | Source | Detail |
|---|---|---|---|---|
| PBI-10 | Rename `host_name` → `hostname` xuyên suốt | proposed | user request 2026-07-16 | Đổi tên field/param `host_name` thành `hostname` ở mọi nơi: `ServerConfig.host_name` (config.rs, kèm serde), `SettingsForm.host_name` + `update_config` + `normalize_host_name` (server.rs), input `name="host_name"` trong settings form (views.rs), các chỗ đọc `cfg.server.host_name` (runtime.rs, mcp.rs), và docs/specs/README. ⚠️ **Backward-compat:** config.toml đang tồn tại dùng key `host_name` → thêm `#[serde(alias = "host_name")]` trên field `hostname` mới để không vỡ config cũ. Đích: `crates/mdview-core/src/config.rs`, `crates/mdview/src/{server,runtime,mcp,views}.rs` + docs. |
| PBI-13 | Bound-port truth: dùng config port nếu được, không thì tự tìm; mọi link theo port thực | proposed | user request 2026-07-16 | Server start: thử `config.server.port`, nếu bận thì auto-increment tìm port trống (đã có `bind_with_retry`). **Yêu cầu cốt lõi:** MỌI link sinh ra (CLI `open`, MCP `mdview_view_file`, web) phải dùng **port thực đã bind**, không phải port config. Happy-path hiện đúng (`running_daemon().port` = `addr.port()` thực); **gap:** `ensure_bind` khi readiness-timeout trả về `cfg.server.port` (có thể sai nếu đã auto-increment) — trùng review-finding reliability đã có trong `.bee/backlog.jsonl` ("readiness timeout returns configured port not bound port"). Việc: đảm bảo port thực lan tới mọi URL builder + fix nhánh timeout. Đích: `crates/mdview/src/{runtime,server}.rs`. |
| PBI-14 | Verify mcp/cli URL cho 1 file: hostname → link theo hostname; không hostname + 0.0.0.0 → n url theo mỗi IP | proposed | user request 2026-07-16 (kiểm tra PBI-04) | Kiểm tra & khẳng định luồng dựng URL khi trả 1 file: nếu `hostname` set → 1 link build theo hostname; nếu KHÔNG có hostname mà bind `0.0.0.0` → detect tất cả IP và tạo **n url, mỗi IP một url**. Logic này đã làm ở PBI-04 (`build_display_urls`/`ensure_daemon_bases`, unit-tested), nhưng user muốn **verify end-to-end** rằng MCP `mdview_view_file` và CLI `open` thực sự trả đúng — thêm e2e/integration coverage cho cả 2 đường, xét cả bound-port thực (PBI-13). Đích: `crates/mdview/src/{mcp,runtime,cli}.rs` + test. |
| PBI-16 | Version xuyên suốt: 1 nguồn sự thật, hiển thị ở CLI + install.sh + settings | proposed | user request 2026-07-16 | **1 nguồn sự thật** cho version = `Cargo.toml` workspace version (`env!("CARGO_PKG_VERSION")`, đã dùng ở `/health`). Mỗi lần release chỉ cập nhật giá trị này (tag `vX.Y.Z` khớp Cargo version; ghi rõ bước bump hoặc tự động hoá từ tag). Hiển thị 3 nơi: (1) **CLI** — `mdview --version` / `mdview version` in version hiện tại (kiểm tra clap đã auto cấp `--version` chưa, thêm subcommand `version` nếu cần); (2) **install.sh** — báo rõ đang install version nào (resolve `latest` → tag thực, hoặc in `MDVIEW_VERSION`); (3) **settings form** — show version (footer/header). Đích: `crates/mdview/src/cli.rs`, `install.sh`, `crates/mdview/src/views.rs` (`settings_page`). |
| PBI-09 | Bundle Manrope font locally (offline typeface fidelity) | done | bundle-manrope-font · [history](history/bundle-manrope-font/) | Atelier design đặc trưng bởi **Manrope** ("Manrope throughout"), nhưng `themes/atelier.css` nạp nó qua `@import url(https://fonts.googleapis.com/...)` — external fetch, vi phạm ràng buộc offline nên đã bị strip khi adopt (fallback về `system-ui`). Việc: vendor Manrope woff2 (OFL, redistributable) vào assets + `@font-face` served qua route, để đạt fidelity đúng typeface mà vẫn offline. Đích: `crates/mdview/assets/` (woff2 + @font-face trong glue/atelier css) + `crates/mdview/src/server.rs` (route phục vụ font). |
| PBI-15 | Tối ưu layout form Settings (gọn, gom nhóm field theo Atelier) | done | polish-settings-form · [history](history/polish-settings-form/) | Form settings hiện **xấu & tùm lum**; sắp lại gọn gàng theo Atelier design system (dùng vocabulary `.fg-*`, token spacing/typography). **Gom field liên quan cùng hàng/fieldset** — ví dụ `host` + `port` chung một hàng; các nhóm khác tự quyết theo ngữ nghĩa (gợi ý: **Server** = host+port+hostname · **Appearance** = theme+syntax_theme · **Indexing** = debounce_ms+max_file_size_mb (cùng hàng) + exclude_patterns (textarea) · **Integration** = mcp_enabled+mcp_transport · **Behavior** = open_browser). Grid responsive, label gọn, giữ nguyên `name=` các field (để `update_config` không đổi). Đích: `crates/mdview/src/views.rs` (`settings_page`) + `crates/mdview/assets/app.css`. |
| PBI-01 | nucleo fuzzy file search (jump-to-file theo tên/đường dẫn) | done | marky:nucleo-fuzzy-search (porting-log) | Wire `nucleo-matcher` (đã có trong Cargo.toml, hiện 0 usage) thành fuzzy search theo tên/đường dẫn file, song song với FTS5 (FTS5 = tìm nội dung; nucleo = nhảy nhanh tới file). Smart-case; haystack = relative path đã index; rebuild index phẳng. Đích khả dĩ: `crates/mdview-core` (matcher) + `crates/mdview/src/server.rs` (endpoint). Port Rust→Rust gần thẳng từ marky. |
| PBI-02 | Copy-as-markdown qua source-map attrs | done | marky:source-map-copy-as-markdown (porting-log) | Chèn `data-source-map` lúc parse để map vùng chọn DOM → dòng markdown gốc; copy ra **markdown thô**, không phải HTML đã render. Cần: attrs lúc parse (`crates/mdview-core/src/render.rs`) + client JS xử lý selection→copy (`crates/mdview/src/views.rs` / static app.js). Differentiator hiếm, hợp workflow agent/dev. |
| PBI-03 | Zoom mermaid diagram (kể cả fullscreen) | done | user request 2026-07-16 | Tìm & đánh giá thư viện cho phép **zoom/pan diagram mermaid, lý tưởng có chế độ fullscreen**. Mermaid hiện render client-side ra SVG (xem "How it works" README). Ứng viên khả dĩ: `svg-pan-zoom`, `@panzoom/panzoom` (anvaka), `d3-zoom`. Việc = research chọn lib + wrap SVG mermaid với controls zoom/pan + nút fullscreen. Đích: client JS + `crates/mdview/src/views.rs` (khối `<script type="module">` mermaid). |
| PBI-04 | Multi-IP URLs khi listen 0.0.0.0 không có host_name | done | user request 2026-07-16 | Khi server bind `0.0.0.0` **và** không có `host_name` override: enumerate mọi IP máy có thể được truy cập (liệt kê network interface, loại loopback/link-local tùy chọn, IPv4 trước), rồi khi MCP `mdview_view_file` trả về, trả **danh sách link cho tất cả IP** (thay vì 1) để agent/user chọn IP nào tới được. ⚠️ Chỉ thay ở chỗ **dựng URL hiển thị** (`base_url()` / MCP response builder), KHÔNG đụng `DaemonInfo.host` dùng cho kết nối thật (critical-pattern: display value ≠ functional value). Nối tiếp feature host_name override. Đích: `crates/mdview/src/mcp.rs` (response), URL builder, + helper enumerate IP (vd crate `local-ip-address` / `if-addrs`). |
| PBI-05 | Command `mdview restart` | done | user request 2026-07-16 | Thêm subcommand `mdview restart` = stop daemon đang chạy (nếu có) rồi start lại một daemon detached mới. Hữu ích sau khi đổi config (host_name, port, theme) để áp dụng ngay. Tái dùng logic stop + spawn-detached sẵn có (setsid/DETACHED_PROCESS). Xử lý gọn khi không có daemon nào đang chạy (chỉ start). Đích: `crates/mdview/src/cli.rs` (subcommand) + `crates/mdview/src/runtime.rs` (stop→spawn). |
| PBI-06 | Fix 4 P1 từ full-app review (feature: fix-review-p1-findings-260716) | done | review session review-2026-07-16-full-app | (1) `asset_path`/`project_path` asset fallback serve bất kỳ file nào dưới project root — siết lại bằng extension allowlist (đúng list `content_type()`, check trên canonical path để tránh symlink bypass) + tôn trọng `exclude_patterns`. (2-4) 3 cell behavior-change đã cap nhưng thiếu automated test (`config-edit-cli-1`, `copy-as-markdown-2`, `mdview-restart-1`) — trích pure function + unit test. Đích: `crates/mdview-core/src/engine.rs`, `crates/mdview/src/{server,cli,views}.rs`. Xem `docs/history/fix-review-p1-findings-260716/`. |
| PBI-07 | Claude Code skill global `/mdview <relative-file-path>` cài bởi `mdview doctor` | done | user request 2026-07-16 | Thêm skill Claude Code (`~/.claude/skills/mdview/SKILL.md` hoặc tương đương) nhận `relative-file-path`, tự đánh giá gọi **CLI** (`mdview open`) hay **MCP** (`mdview_view_file`) là phương pháp tốt nhất, rồi trả về url(s) xem file đó — tuân theo nguyên tắc dựng URL hiện có (multi-IP khi bind `0.0.0.0` không `host_name`, display value tách khỏi functional value — xem PBI-04 và critical-pattern `DaemonInfo.host`). Cài đặt **global** (không phải per-project), do `mdview doctor` cài/kiểm tra idempotent — mở rộng theo pattern check hiện có trong `crates/mdview/src/doctor.rs` (`check_mcp_registration`, `write_agent_snippet`/`check_agent_instruction`). Đích khả dĩ: `crates/mdview/src/doctor.rs` (check mới) + nội dung skill file mới. Xếp hàng, chưa thực thi. |
| PBI-08 | Thay toàn bộ design bằng Atelier design system (feature: adopt-atelier-design-system) | done | user request 2026-07-16 · [history](history/adopt-atelier-design-system/) | Thay toàn bộ design custom của web UI (`crates/mdview/assets/app.css`, theme logic trong `app.js`, markup trong `views.rs`) bằng **Atelier design system** ở `docs/design/` (contract/ token tiers + `themes/atelier.css` light+dark + patterns, đặc biệt `editorial` cho markdown-body). Re-express markup bằng vocabulary `.fg-*` (không phải re-skin class cũ). Giữ toggle **Dark/Light** (map cơ chế `data-theme` hiện tại → `data-theme="atelier"` + `data-scheme="light\|dark"`, giữ no-flash + OS default + localStorage). Chỉ expose axis scheme (bỏ accent/density/typeface). Giữ syntect `/highlight.css` phối hợp light/dark. Desktop splash (`crates/mdview-desktop/ui/index.html`) restyle theo token. Xem `docs/history/adopt-atelier-design-system/CONTEXT.md`. |
| PBI-11 | BUG: copy-as-markdown không chạy (regression) | done | fix-atelier-regressions · [history](history/fix-atelier-regressions/) | Người dùng báo copy-as-markdown không chạy. Nghi vấn chính: **PBI-08 (Atelier) viết lại markup `views.rs` + `app.js`** → handler JS mất hook vào DOM (class đổi sang `.fg-prose`, `data-sourcepos`, `#mdsource`). Việc: repro, kiểm tra client JS trong `app.js` (selection → dùng `data-sourcepos` map về source trong `#mdsource`) còn khớp markup mới không; fix wiring. Đích: `crates/mdview/assets/app.js` + có thể `crates/mdview/src/views.rs`. |
| PBI-12 | BUG: zoom mermaid không hoạt động (regression) | done | fix-atelier-regressions · [history](history/fix-atelier-regressions/) | Người dùng báo zoom mermaid không chạy. Nghi vấn: (a) **PBI-08 restyle controls mermaid-zoom trong app.css** (commit d3171a6) làm gãy wiring/selector của control zoom/pan/fullscreen trong `app.js`; (b) mermaid nạp từ **CDN jsdelivr** (`views.rs` khối `<script type=module>`) → nếu offline thì mermaid không render, không có gì để zoom. Việc: repro (online), kiểm tra control zoom/pan còn bind đúng SVG mermaid sau restyle; xét cả trường hợp offline. Đích: `crates/mdview/assets/app.js` + `crates/mdview/src/views.rs`. |

<!-- Các PBI do người dùng chốt adopt, xếp hàng cho session đang chạy nhặt khi xong việc hiện tại. Không thực thi ngay. -->
